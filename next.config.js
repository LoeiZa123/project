/** @type {import('next').NextConfig} */
const nextConfig = {};

// next.config.js
module.exports = {
    images: {
      domains: ['heroui.com'],  // เพิ่มโดเมนที่ต้องการให้ Next.js โหลดภาพจากที่นี่
    },
}